<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.storageandpermissiontask6">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StorageAndPermissionTask6">
        <activity android:name=".editcontacts.EditContactActivity"></activity>
        <activity android:name=".readcontacts.ReadContactsActivity" />
        <activity android:name=".ui.ContactDetails" />
        <activity android:name=".ui.AddContact" />
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.ContactsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>